import 'package:flutter/material.dart';
import 'app.dart';

/// PUBLIC_INTERFACE
void main() {
  /// App entrypoint: runs the MicroLearningApp with Ocean Professional theme
  /// and bottom tab navigation (Home, Learn, Quizzes, Profile).
  runApp(const MicroLearningApp());
}
